# 随访周期佩戴日历系统说明与服务器交接文档

> **版本**：Fupwc-1.00.03  |  **更新日期**：2026-08-14  |  **编译环境**：JDK 8 + Maven 3.x  |  **核心依赖**：Apache POI 4.1.2

---

## 一、系统架构与模块构成

系统基于 **JDK 8** 开发，与服务器 `taurus` 系统的运行环境完全一致。

### 包结构总览

```
com.calendar
├── Main.java                          // 程序入口（仅 GUI 客户端使用）
├── model/
│   ├── DetectionTask.java            // 单个检测任务（随访周期）结构体
│   └── WearingCalendarRule.java      // 整体随访规则模型
├── service/
│   ├── DateCalculator.java           // 日期与随访日历推算引擎（V1.1 锚定日算法）
│   └── WearingCalendarService.java   // Excel 导入导出服务（服务器可直接调用）
└── gui/
    ├── MainFrame.java                // 主窗口框架（Nimbus 高清界面风格）
    ├── RuleAddPanel.java             // 规则添加面板（含自定义检测周期覆盖）
    └── RuleTestPanel.java            // 规则测试面板（含 JSON 输出与日期推算）
```

> 服务器端只需关注 `model` 和 `service` 包下的 4 个类，`gui` 包和 `Main.java` 为客户端专用。

---

## 二、桌面客户端运行与测试说明

### 1. 启动方式

| 平台 | 启动方式 |
| :--- | :--- |
| **Mac** | 双击 `output/FollowUpCalendar.app`，或运行 `./start-mac.sh` |
| **Windows** | 双击 `start-win.bat`（自带 jre 免安装） |
| **通用** | `java -jar target/Follow-up-period-wearing-calendar-Fupwc-1.00.03-jar-with-dependencies.jar` |

### 2. 功能测试流程

**规则添加模块**：
1. 打开【规则添加】页签
2. 设置：周期数值、单位类型（天/周/月/年）、检测次数
   - 检测次数按单位动态限制上限：天 ≤ 1×N、周 ≤ 7×N、月 ≤ 31×N、年 ≤ 365×N
3. 点击【添加规则】，右侧实时显示规则列表
4. 支持【删除选中】和【清空规则】操作
5. 底部显示检测周期信息：
   - **默认**：显示所有随访周期实际相加的总天数（如 `"所有周期加起来总天数为 365 天 (自动换算: 1 年)"`）
   - **勾选 `[x] 使用自定义检测周期`**：启用右侧数值输入框和单位下拉框（天/周/个月/年），手动覆盖 `detectionPeriodNum` 和 `detectionPeriodUnit`
6. 点击【生成 Excel 规则文件】导出标准 `.xlsx` 文件
7. 点击【直接测试】一键传入测试模块

**规则测试模块**：
1. 打开【规则测试】页签
2. 点击【导入规则】选择 Excel 规则文件
3. 左侧显示解读出的规则列表和检测周期
4. 右侧选择三组日期（年、月、日下拉框）：
   - **佩戴起始日**（`startWearingDate`）：参与算法推算
   - **手术日期**（`surgeryDate`）：只存变量，JSON 查看可见
   - **建议佩戴日**（`suggestWearingDate`）：只存变量，JSON 查看可见
   - 三个日期初始值均为 `null`，界面显示为 `"--"`
5. 系统推算并在表格中展示每个佩戴周期的起始时间和结束时间
6. 点击【查看JSON数据】弹出完整的格式化 JSON 数据结构，支持一键复制到剪贴板

---

## 三、核心数据模型（对应设计文档整体设计）

### 3.1 `WearingCalendarRule` — 整体随访规则模型

| 变量名 | 类型 | 说明 | 默认值 | 来源 |
| :--- | :--- | :--- | :--- | :--- |
| `WearMode` | `int` | 佩戴模式：`0`=固定日期, `1`=窗口期任意日 | `0` | 医生输入 |
| `WearType` | `int` | 佩戴类型：`0`=固定模式, `1`=自定义模式 | `0` | 建立课题时输入 / 系统自动计算 |
| `surgeryDate` | `String` | 手术日期（提醒医生，与运算关系不大），格式 `yyyy-MM-dd` | `null` | 医生输入 |
| `startWearingDate` | `String` | 佩戴起始日（参与整个运算），格式 `yyyy-MM-dd` | `null` | 医生输入 |
| `suggestWearingDate` | `String` | 建议佩戴日（提醒医生，与运算关系不大），格式 `yyyy-MM-dd` | `null` | 医生输入 |
| `detectionPeriodNum` | `int` | 检测周期数值（由系统自动推算或自定义覆盖） | `0` | 系统推算 / 自定义输入 |
| `detectionPeriodUnit` | `int` | 检测周期单位：`0`=天, `1`=周, `2`=月, `3`=年 | `0` | 系统推算 / 自定义输入 |
| `useCustomDetectionPeriod` | `boolean` | 是否使用自定义检测周期覆盖（`true`=使用用户指定值，`false`=使用系统自动推算值） | `false` | GUI 勾选框 / 服务器前端设置 |
| `detectionTaskList` | `List<DetectionTask>` | 检测任务列表（每个元素代表一个随访周期） | 空列表 | 导入 Excel 或 GUI 添加得出 |

> **`detectionPeriodNum` + `detectionPeriodUnit` 计算与覆盖规则**：
> - **`useCustomDetectionPeriod = false`（默认）**：系统计算所有随访周期加起来的总天数（`tDays`），按优先级转化：能整除365→年，能整除30→月，能整除7→周，其余→天。
> - **`useCustomDetectionPeriod = true`**：跳过自动推算，直接使用前端/GUI 填入的 `detectionPeriodNum` 和 `detectionPeriodUnit` 值。
>
> **`WearType` 自动计算逻辑**：如果 `detectionTaskList` 中所有任务的 `detectionFrequencyUnit`、`detectionFrequencyNum`、`detectionFrequencyCount` 完全相同则为 `0`（固定），否则为 `1`（自定义）。

### 3.2 `DetectionTask` — 单个检测任务（随访周期）

| 变量名 | 类型 | 说明 | 默认值 | 来源 |
| :--- | :--- | :--- | :--- | :--- |
| `taskName` | `String` | 周期名称（如 "第一周期"） | `null` | 自动生成 / 导入得出 |
| `startDate` | `String` | 周期开始日期，格式 `yyyy-MM-dd` | `null` | 根据 `startWearingDate` 推算 |
| `endDate` | `String` | 周期结束日期，格式 `yyyy-MM-dd` | `null` | 根据规则推算 |
| `taskNum` | `int` | 任务编号排序（从 1 开始） | `0` | 自动编号 |
| `wearingDate` | `String` | 佩戴日（暂定） | `null` | 暂定 |
| `overdueDays` | `int` | 超期天数 | `0` | 暂定 |
| `status` | `int` | 任务状态：`-1`超期未佩戴, `0`未佩戴, `1`已佩戴 | `1` | 暂定 |
| `detectionFrequencyNum` | `int` | 检测频率值（周期数值，如 "2周" 中的 2） | `0` | 医生输入 / 导入得出 |
| `detectionFrequencyUnit` | `int` | 检测频率单位：`0`=天, `1`=周, `2`=月, `3`=年 | `0` | 医生输入 / 导入得出 |
| `detectionFrequencyCount` | `int` | 检测频率次数 | `0` | 医生输入 / 导入得出 |
| `validDetectionDuration` | `int` | 有效检测时长 | `6` | 默认 6 |
| `validDetectionDurationUnit` | `int` | 有效检测时长单位：`0`=分钟, `1`=小时 | `1` | 默认 1（小时） |

> **重要说明**：
> - Excel 表中的起始时间和结束时间是例子显示（以当天为起始日计算），不是上面任何一个变量。`startDate` 和 `endDate` 需要在后面有人填写佩戴起始日（`startWearingDate`）后才进行实际计算。
> - 检测次数上限动态校验：天 ≤ `1 × detectionFrequencyNum`，周 ≤ `7 × detectionFrequencyNum`，月 ≤ `31 × detectionFrequencyNum`，年 ≤ `365 × detectionFrequencyNum`。

---

## 四、月度随访日历推算算法（V1.1 锚定日机制）

当检测频率单位为**月（`detectionFrequencyUnit == 2`）**时，系统采用基于**锚定日（Anchor Day）**的精准推算算法。

### 1. 核心规则

- 以 `startWearingDate` 的"日"（`dayOfMonth`）作为全局 **`anchorDay`**（取值范围 1 ~ 31）。
- 推算下 N 个月对应的目标年份和月份后，获取该目标月份的实际最大天数 `maxDays`。

**分两种情况处理：**

1. **常规情况（`maxDays >= anchorDay`）**：
   - 下一周期起始日 = 目标月份的 `anchorDay`
   - 当前周期结束日 = 下一周期起始日 - 1 天

2. **特殊情况（`maxDays < anchorDay`）**：如平年2月（28天）遇到 anchorDay=29/30/31
   - 下一周期起始日 = 目标月份的最后一天（`maxDays`）
   - 当前周期结束日 = 目标月份最后一天的前一天（`maxDays - 1`）
   - **锚定日不衰减**：后续月份若存在 `anchorDay`，自动恢复为 `anchorDay`

### 2. 算法核心代码片段（`DateCalculator.java` 第 120-127 行）

```java
case 2: // 月 (锚定日 V1.1 算法)
    LocalDate targetMonthDate = curStart.plusMonths(cnt);
    int maxDays = targetMonthDate.lengthOfMonth();
    int targetDay = Math.min(anchorDay, maxDays);
    nextStart = LocalDate.of(targetMonthDate.getYear(), targetMonthDate.getMonthValue(), targetDay);
    curEnd = nextStart.minusDays(1);
    break;
```

### 3. 边界用例推导验证

#### 示例 A：佩戴起始日 `2026-01-31`（平年，`anchorDay = 31`）

| 周期 | 起始日 | 结束日 | 推算说明 |
| :--- | :--- | :--- | :--- |
| 第一周期 | `01-31` | `02-27` | 2月 `maxDays=28 < 31`，下期=2月28日，本期结束=27日 |
| 第二周期 | `02-28` | `03-30` | 3月 `maxDays=31 >= 31`，恢复31日，本期结束=30日 |
| 第三周期 | `03-31` | `04-29` | 4月 `maxDays=30 < 31`，下期=4月30日，本期结束=29日 |
| 第四周期 | `04-30` | `05-30` | 5月 `maxDays=31 >= 31`，恢复31日，本期结束=30日 |

#### 示例 B：佩戴起始日 `2024-01-31`（闰年，`anchorDay = 31`）

| 周期 | 起始日 | 结束日 | 推算说明 |
| :--- | :--- | :--- | :--- |
| 第一周期 | `01-31` | `02-28` | 闰年2月 `maxDays=29 < 31`，下期=2月29日，本期结束=28日 |
| 第二周期 | `02-29` | `03-30` | 3月 `maxDays=31 >= 31`，恢复31日，本期结束=30日 |

#### 示例 C：佩戴起始日 `2026-11-29`（平年，`anchorDay = 29`）

| 周期 | 起始日 | 结束日 | 推算说明 |
| :--- | :--- | :--- | :--- |
| 第一周期 | `11-29` | `12-28` | 12月 `maxDays=31 >= 29`，正常取29日 |
| 第二周期 | `12-29` | `01-28` | 1月 `maxDays=31 >= 29`，正常取29日 |
| 第三周期 | `01-29` | `02-27` | 平年2月 `maxDays=28 < 29`，下期=2月28日，本期结束=27日 |
| 第四周期 | `02-28` | `03-28` | 3月 `maxDays=31 >= 29`，恢复29日，本期结束=28日 |
| 第五周期 | `03-29` | `04-28` | 4月 `maxDays=30 >= 29`，正常取29日 |

#### 示例 D：佩戴起始日 `2026-11-30`（平年，`anchorDay = 30`）

| 周期 | 起始日 | 结束日 | 推算说明 |
| :--- | :--- | :--- | :--- |
| 第一周期 | `11-30` | `12-29` | 12月 `maxDays=31 >= 30`，正常取30日 |
| 第二周期 | `12-30` | `01-29` | 1月 `maxDays=31 >= 30`，正常取30日 |
| 第三周期 | `01-30` | `02-27` | 平年2月 `maxDays=28 < 30`，下期=2月28日，本期结束=27日 |
| 第四周期 | `02-28` | `03-29` | 3月 `maxDays=31 >= 30`，恢复30日，本期结束=29日 |
| 第五周期 | `03-30` | `04-29` | 4月 `maxDays=30 >= 30`，正常取30日 |

---

## 五、服务器后端对接与 API 调用指南

### 1. Jar 包引入

将编译生成的 `Follow-up-period-wearing-calendar-Fupwc-1.00.03-jar-with-dependencies.jar`（含 Apache POI）放入服务器 classpath 或通过 Maven 本地安装引入。

### 2. API 调用示例

#### 2.1 组装规则并推算个人佩戴日历

```java
import com.calendar.model.WearingCalendarRule;
import com.calendar.model.DetectionTask;
import com.calendar.service.DateCalculator;

// 1. 构建规则对象（通常由前端 JSON 反序列化而来）
WearingCalendarRule rule = new WearingCalendarRule();
rule.setSurgeryDate("2026-07-25");
rule.setSuggestWearingDate("2026-07-28");

// 2. 添加检测任务
// 参数：(detectionFrequencyUnit, detectionFrequencyNum, detectionFrequencyCount)
// 单位枚举：0=天, 1=周, 2=月, 3=年
rule.addDetectionTask(new DetectionTask(2, 1, 1)); // 每1月检测1次
rule.addDetectionTask(new DetectionTask(2, 2, 2)); // 每2月检测2次

// 3. 传入佩戴起始日，推算所有周期的起始与结束时间
DateCalculator.calculatePeriods(rule, "2026-07-30");

// 4. 获取结果
System.out.println(rule.toJsonString());
```

#### 2.2 从 Excel 导入规则

```java
import com.calendar.service.WearingCalendarService;
import com.calendar.model.WearingCalendarRule;
import java.io.File;

// 从文件导入
WearingCalendarRule rule = WearingCalendarService.parseExcel(new File("随访周期规则.xlsx"));

// 从输入流导入（适用于服务器上传文件）
WearingCalendarRule rule2 = WearingCalendarService.parseExcel(inputStream);
```

#### 2.3 导出 Excel

```java
import com.calendar.service.WearingCalendarService;
import java.io.File;

// 导出为文件
WearingCalendarService.exportExcel(rule, new File("output.xlsx"));

// 导出为字节数组（适用于 HTTP 下载）
byte[] excelBytes = WearingCalendarService.exportExcelToBytes(rule);
```

#### 2.4 获取 Jar 包版本信息

Jar 包内置了版本输出接口与命令行支持：

```java
import com.calendar.service.WearingCalendarService;
import com.calendar.Main;

// 方法 1：通过 Service 静态 API 获取版本字符串
String version = WearingCalendarService.getVersion(); // 返回 "Fupwc-1.00.03"

// 方法 2：通过 Main 静态 API 获取版本字符串
String mainVersion = Main.getVersion(); // 返回 "Fupwc-1.00.03"
```

**命令行获取版本**：
```bash
java -jar target/Follow-up-period-wearing-calendar-Fupwc-1.00.03-jar-with-dependencies.jar --version
# 输出：Fupwc-1.00.03
```


---

## 六、Excel 文件存储与解析规范

### Excel 表格结构（Sheet: "随访佩戴规则"）

| 行号 | A列 | B列 | C列 | D列 | F列 | G列 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 第1行 | `起始日` | `2026-07-30` | | | | |
| 第2行 | `检测规则` | `0: 固定` 或 `1: 自定义` | `0` 或 `1`（WearType 数值） | | | |
| 第3行 | | `1: 自定义` 或 `0: 固定`（辅助提示） | | | | |
| 第4行 | `佩戴周期` | `检测周期` | `单位` | `次数` | `起始时间` | `结束时间` |
| 第5行起 | `1` | `1` | `月` | `1` | `2026-07-30` | `2026-08-29` |
| 最后一行 | | | `检测周期` | `1 年` | | |

> **解析逻辑说明**：
> - 读取从**第5行（row index 4）** 开始逐行读取数据，遇到 C 列为 `"检测周期"` 或 `"总天数"` 时停止。
> - A列为空且B列为空且C列为空的行自动跳过。
> - F列（起始时间）和 G列（结束时间）为例子展示，实际使用时需通过 `DateCalculator.calculatePeriods()` 传入真实的 `startWearingDate` 重新推算。

---

## 七、数据结构 JSON 格式输出示例

```json
{
  "WearMode": 0,
  "WearType": 1,
  "surgeryDate": "2026-07-25",
  "startWearingDate": "2026-07-30",
  "suggestWearingDate": "2026-07-28",
  "detectionPeriodNum": 90,
  "detectionPeriodUnit": 0,
  "useCustomDetectionPeriod": false,
  "detectionTaskList": [
    {
      "taskName": "第一周期",
      "startDate": "2026-07-30",
      "endDate": "2026-08-29",
      "taskNum": 1,
      "wearingDate": null,
      "overdueDays": 0,
      "status": 1,
      "detectionFrequencyNum": 1,
      "detectionFrequencyUnit": 2,
      "detectionFrequencyCount": 1,
      "validDetectionDuration": 0,
      "validDetectionDurationUnit": 0
    },
    {
      "taskName": "第二周期",
      "startDate": "2026-08-30",
      "endDate": "2026-10-28",
      "taskNum": 2,
      "wearingDate": null,
      "overdueDays": 0,
      "status": 1,
      "detectionFrequencyNum": 2,
      "detectionFrequencyUnit": 2,
      "detectionFrequencyCount": 2,
      "validDetectionDuration": 0,
      "validDetectionDurationUnit": 0
    }
  ]
}
```

---

## 八、命令行编译打包指南

### 1. Maven 编译

```bash
cd Follow-up-period-wearing-calendar
mvn clean package -DskipTests
```

编译成功后将在 `target/` 目录下生成：
- `Follow-up-period-wearing-calendar-Fupwc-1.00.03-jar-with-dependencies.jar`（含所有依赖的 Fat Jar）

### 2. Mac App 资源嵌入

编译完成后，需将 Jar 拷贝到 App 包内部：
```bash
cp target/Follow-up-period-wearing-calendar-Fupwc-1.00.03-jar-with-dependencies.jar output/FollowUpCalendar.app/Contents/Resources/
```

### 3. 整体打包

```bash
zip -r Follow-up-period-wearing-calendar.zip . -x "*.DS_Store" -x "*.git*"
```

---

## 九、文件夹结构说明

```
项目文件夹/
├── start-win.bat                  ← Windows 一键启动脚本（推荐）
├── start-mac.sh                   ← Mac 启动脚本
├── pom.xml                        ← Maven 构建配置
├── launch4j-config.xml            ← Launch4j EXE 生成配置
├── jre/                           ← 内置 JRE 8 运行环境（Windows 免安装）
├── target/
│   └── Follow-up-period-wearing-calendar-Fupwc-1.00.03-jar-with-dependencies.jar
├── output/
│   ├── FollowUpCalendar.exe       ← Windows EXE（需先用 Launch4j 生成）
│   └── FollowUpCalendar.app/      ← Mac 应用（内含嵌入式 Jar 资源）
├── src/                           ← 源代码
├── Launch4j/                      ← Launch4j 工具（用于生成 Windows EXE）
├── 佩戴周期设计文档/               ← 设计文档与参考图
├── 系统说明与服务器交接文档.md      ← 本文档
└── Windows系统使用与运行指南.md    ← Windows 端用户使用指南
```

---

## 十、常见问题与环境故障排查 FAQ

### Q1：双击 `start-win.bat` 闪退
**原因**：系统未找到 Java 运行环境。  
**解决**：确保 `jre/` 文件夹完整保留在项目根目录下。`start-win.bat` 会优先使用 `jre/bin/javaw.exe`，如未找到则尝试系统 PATH 中的 `java` 命令。

### Q2：Mac App 双击打不开
**原因**：未赋予执行权限。  
**解决**：运行 `chmod +x output/FollowUpCalendar.app/Contents/MacOS/FollowUpCalendar`，或使用 `./start-mac.sh` 启动。

### Q3：Excel 导入后日期显示异常
**原因**：Excel 中的日期被存储为数字格式而非文本。  
**解决**：系统已内置 `DateUtil.isCellDateFormatted()` 自动识别 Excel 日期格式并转化为 `yyyy-MM-dd` 字符串，同时支持 `yyyy年M月d日`、`yyyy-MM-dd`、`yyyy/MM/dd` 三种文本格式兼容解析。

### Q4：中文显示乱码
**原因**：终端或 IDE 编码与 Java 不一致。  
**解决**：确保所有源文件保存为 **UTF-8** 编码。`pom.xml` 中已配置 `<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>`。

### Q5：日期格式统一标准
系统中所有日期字符串统一采用 `yyyy-MM-dd`（以 `-` 分割年月日），如 `2026-07-30`。`DateCalculator.DISPLAY_FORMATTER` 定义为 `DateTimeFormatter.ofPattern("yyyy-MM-dd")`。

---

## 附：版本演进历史

| 版本 | 日期 | 主要变更 |
| :--- | :--- | :--- |
| v1.0.0 | 2026-07 | 初始版本，包含基础随访周期规则添加与 Excel 导入导出 |
| v2.0.0 | 2026-07-29 | 重构数据模型（`MonitorEcgStruct` → `DetectionTask`），新增 `surgeryDate`/`startWearingDate`/`suggestWearingDate` 三日期变量，新增 JSON 数据查看，移除季度单位，日期格式统一为 `yyyy-MM-dd`，动态检测频率上限校验 |
| v2.1.0 | 2026-08-03 | 月度推算升级为 V1.1 锚定日（Anchor Day）算法，新增 `useCustomDetectionPeriod` 自定义检测周期覆盖机制，界面采用 Nimbus 高清风格 |
| v2.1.1 | 2026-08-04 | 优化周期自动推算算法为同单位精确累加机制（解决 6 个月误显示为 6 天的单位换算边缘 Bug），实现全流程文本框即打即显与下拉框即选即推算 |
| Fupwc-1.00.01 | 2026-08-12 | 版本号加入 Fupwc 项目简称，新增 Jar 包内部 `getVersion()` 静态 API 与命令行 `--version` 输出，打包文件名同步更新 |
| Fupwc-1.00.02 | 2026-08-14 | 修复自定义检测周期导出/导入时被重置为自动推算天数的 Bug，修复单位包含“个”（如“36个月”）解析时误回退为“天”的 Bug，递增版本号 |
| Fupwc-1.00.03 | 2026-08-14 | GUI 桌面界面增加主窗口标题栏与底部右下角状态栏版本号显示，递增版本号 |
