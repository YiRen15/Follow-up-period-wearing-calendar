# Windows 系统使用与零配置部署指南

> **版本**：Fupwc-1.00.02  |  **更新日期**：2026-08-14  
> 本文档专为 Windows 用户准备，包含零安装启动、功能操作、新功能使用和常见问题解答。

---

## 一、快速开始（3 步上手）

### 第 1 步：解压文件
- 将收到的 `.zip` 压缩包解压到任意目录。

### 第 2 步：选择运行版本与启动

| 版本 | 启动方式 | 适用场景 |
| :--- | :--- | :--- |
| **标准完整版** | 双击 `start-win.bat`（推荐），或双击 `output/FollowUpCalendar.exe` | 完整随访规则添加、月度日历推算测试、JSON 查看与日期模拟 |
| **独立精简版** | 进入 `Follow-up-period-wearing-calendar-simple` 目录，双击 `start-win.bat` | 仅需规则添加与 Excel 导出/极简展示列表 |

### 第 3 步：开始使用
- 程序启动后会自动弹出软件界面（默认采用现代高清 Nimbus 界面）。
- 左侧为操作输入区，右侧为规则数据与结果显示区。

---

## 二、零配置免安装说明

### 项目自带运行环境

本程序文件夹内已内置 **`jre` 文件夹**（Windows 64 位 JRE 8 运行环境），因此：

- ✅ **无需安装 Java**
- ✅ **无需配置环境变量**
- ✅ **无需安装 Maven 或其他开发工具**
- ✅ 解压后双击 `start-win.bat` 即可直接运行

---

## 三、新功能操作指南

### 3.1 规则添加与【自定义检测周期】

1. 打开 **【规则添加】** 页签
2. 设置：**周期数值**、**单位类型**（天/周/月/年）、**检测次数**
3. 点击 **【添加规则】** 按钮，右侧表格实时显示新添加的规则
4. **检测周期显示与覆盖**：
   - **默认状态**：底部显示所有随访周期实际相加的**总天数**（如 `"所有周期加起来总天数为 365 天 (自动换算: 1 年)"`）。
   - **手动覆盖状态**：勾选 `[x] 使用自定义检测周期`，解锁后面的数字框与单位下拉框。用户填入数字（如 `1`）与单位（如 `年`），系统将优先以您自定义的周期呈现，并在导出 Excel 与传输 JSON 时自动携带该覆盖值。
5. 点击 **【生成 Excel 规则文件】** 导出标准 `.xlsx` 文件。
6. 点击 **【直接测试】** 按钮，将当前规则一键传入规则测试模块。

### 3.2 规则测试与 V1.1 锚定日历推算

1. 打开 **【规则测试】** 页签。
2. 点击 **【导入规则】** 选择标准 Excel 规则文件。
3. 在右侧选择 **佩戴起始日**（年、月、日 下拉框）：
   - 系统采用 **V1.1 锚定日（Anchor Day）机制**：以佩戴起始日的“日”（如 25号、29号、30号、31号）锚定每月佩戴日；
   - 若遇到 2月（28天/29天）或小月（30天）等不存在锚定日的月份，系统自动将下一周期起始日设为该月最后一天，本期结束日设为最后一天的前一天；
   - 跨过特殊月份后，自动恢复为原始锚定日。
4. 可点击 **【查看 JSON 数据】** 查看传给服务器系统的完整数据结构，并支持一键复制到剪贴板。

---

## 四、文件夹结构说明

```
项目文件夹/
├── start-win.bat                  ← Windows 一键启动脚本（推荐）
├── start-mac.sh                   ← Mac 启动脚本
├── pom.xml                        ← Maven 构建配置
├── launch4j-config.xml            ← Launch4j EXE 生成配置
├── jre/                           ← 内置 JRE 8 运行环境（免安装）
├── target/
│   └── Follow-up-period-wearing-calendar-Fupwc-1.00.02-jar-with-dependencies.jar
├── output/
│   ├── FollowUpCalendar.exe       ← Windows EXE
│   └── FollowUpCalendar.app/      ← Mac 应用
└── src/                           ← 源代码
```
